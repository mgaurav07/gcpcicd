steps:
# Build docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'asia-southeast1-docker.pkg.dev/gcp-lab-p1/gaurav-artifact-repo/app:$COMMIT_SHA', '.']
    
# Push Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'asia-southeast1-docker.pkg.dev/gcp-lab-p1/gaurav-artifact-repo/app:$COMMIT_SHA',]

# Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - run
      - deploy
      - gcp-devops-service
      - --image=asia-southeast1-docker.pkg.dev/gcp-lab-p1/gaurav-artifact-repo/app:$COMMIT_SHA
      - --region=asia-southeast1
      - --platform=managed
      - --allow-unauthenticated

#options:
  #logging: NONE
options:
  logging: CLOUD_LOGGING_ONLY

